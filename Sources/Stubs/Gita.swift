import Foundation

/// Gita stub for SeerMini, this handles the actual contribution calculation of
/// each result and its impact on a LLM response. The search is part 1 (SeerMini), the
/// personalized chat response with royalties is part 2 available in the full Seer
/// implementation.
enum Gita {
    typealias Credits = Double
}
